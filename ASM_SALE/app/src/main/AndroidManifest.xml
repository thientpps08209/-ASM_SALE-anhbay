<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.asm_sale">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Screen.GRIVEWActivity" />
        <activity android:name=".Screen.Product06Activity" />
        <activity android:name=".Screen.Product05Activity" />
        <activity android:name=".Screen.Product04Activity" />
        <activity android:name=".Screen.Product03Activity" />
        <activity android:name=".Screen.Product02Activity" />
        <activity android:name=".Screen.Product01sActivity" />
        <activity android:name=".Screen.GiaodienActivity" />
        <activity android:name=".Screen.RegisterActivity" />
        <activity android:name=".Screen.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>